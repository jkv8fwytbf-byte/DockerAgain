# Week 1 homework

Due Friday. Save your finished notebook in your `submit/` folder.

1. Open `01-python-and-data.ipynb` from `shared/` (copy it into `notebooks/` first if you want to edit it).
2. Draw one extra plot: the class **quiz** scores as a histogram.
3. In a markdown cell, write two sentences about which group (A or B) did better on the project.
4. Optional: run `03-computer-vision.ipynb` and save the edge-detected picture into `submit/` as well.

Name your hand-in file like `week-1-yourname.ipynb`.
